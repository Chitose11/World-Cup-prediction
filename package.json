{
  "name": "sporttery-v32-workbench",
  "version": "0.6.1",
  "private": true,
  "description": "V4 quantitative sports strategy workbench with online research and DeepSeek-V4-Flash recommendations.",
  "type": "module",
  "main": "src/electron-main.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "node --test",
    "desktop": "electron .",
    "dist": "electron-builder --win portable"
  },
  "build": {
    "appId": "local.worldcup.v32.workbench",
    "productName": "V4 Quant Strategy Workbench",
    "directories": {
      "output": "dist"
    },
    "files": [
      "package.json",
      "README.md",
      "src/**/*",
      "public/**/*",
      "model/**/*"
    ],
    "asar": true,
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-portable.${ext}"
    }
  },
  "devDependencies": {
    "electron": "^37.2.5",
    "electron-builder": "^26.0.12"
  }
}
